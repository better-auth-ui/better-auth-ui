---
title: useRequestPasswordResetOtp
description: Email a password-reset code.
---

Pair with `useResetPasswordOtp`, which takes the code and the new password together — no reset link is involved.

## Usage

```tsx
import {
  type EmailOtpAuthClient,
  useAuth,
  useRequestPasswordResetOtp
} from "@better-auth-ui/react"

const { authClient } = useAuth()
const { mutate: requestPasswordResetOtp } = useRequestPasswordResetOtp(
  authClient as EmailOtpAuthClient
)

requestPasswordResetOtp({ email: "alice@example.com" })
```

`useRequestPasswordResetOtp` requires the Better Auth `emailOTP` plugin. Cast `authClient` to `EmailOtpAuthClient` so TypeScript picks up the plugin-extended method signature.

## Options factory

```tsx
import { requestPasswordResetOtpOptions } from "@better-auth-ui/react"
import { useMutation } from "@tanstack/react-query"

const { mutate } = useMutation(requestPasswordResetOtpOptions(authClient))
```

## Params

<auto-type-table path="../../../../../../packages/react/src/mutations/email-otp/request-password-reset-otp-mutation.ts" name="RequestPasswordResetOtpParams" />
