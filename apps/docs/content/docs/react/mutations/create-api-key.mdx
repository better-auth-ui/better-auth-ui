---
title: useCreateApiKey
description: Create an API key for the current user.
---

Requires the Better Auth API key plugin.

## Usage

```tsx
import { useAuth, useCreateApiKey } from "@better-auth-ui/react"

const { authClient } = useAuth()
const mutation = useCreateApiKey(authClient)

mutation.mutate(/* params */)
```

## Options factory

```tsx
import { createApiKeyOptions } from "@better-auth-ui/react"
import { useMutation } from "@tanstack/react-query"

const mutation = useMutation(createApiKeyOptions(authClient))
```

## Params

<auto-type-table path="../../../../../../packages/react/src/hooks/mutations/use-create-api-key.ts" name="CreateApiKeyParams" />
