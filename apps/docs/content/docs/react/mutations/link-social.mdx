---
title: useLinkSocial
description: Link a social provider to the current user.
---

Kicks off the OAuth redirect — the linked account appears in `useListAccounts` after the provider redirects back.

## Usage

```tsx
import { useAuth, useLinkSocial } from "@better-auth-ui/react"

const { authClient } = useAuth()
const { mutate: linkSocial } = useLinkSocial(authClient)

linkSocial({
  provider: "github",
  callbackURL: "/account"
})
```

## Options factory

```tsx
import { linkSocialOptions } from "@better-auth-ui/react"
import { useMutation } from "@tanstack/react-query"

const { mutate } = useMutation(linkSocialOptions(authClient))
```

## Params

<auto-type-table path="../../../../../../packages/core/src/mutations/link-social-mutation.ts" name="LinkSocialParams" />
