---
title: useSignInEmail
description: Email/password sign-in mutation.
---

Resets the session query on success so the new session is refetched.

## Usage

```tsx
import { useAuth, useSignInEmail } from "@better-auth-ui/react"

const { authClient } = useAuth()
const { mutate: signInEmail, isPending } = useSignInEmail(authClient)

signInEmail({
  email: "alice@example.com",
  password: "hunter2",
  rememberMe: true
})
```

## Options factory

```tsx
import { signInEmailOptions } from "@better-auth-ui/core"
import { useMutation } from "@tanstack/react-query"

const { mutate } = useMutation(signInEmailOptions(authClient))
```

## Params

<auto-type-table path="../../../../../../packages/core/src/mutations/sign-in-email-mutation.ts" name="SignInEmailParams" />
