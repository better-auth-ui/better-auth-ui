---
title: useCheckSlug
description: Check whether an organization slug is available.
---

Organization mutations require `organizationClient()` on the Better Auth client.

## Usage

```tsx
import { useAuth, useCheckSlug } from "@better-auth-ui/react"

const { authClient } = useAuth()
const mutation = useCheckSlug(authClient)

mutation.mutate(/* params */)
```

## Options factory

```tsx
import { checkSlugOptions } from "@better-auth-ui/react"
import { useMutation } from "@tanstack/react-query"

const mutation = useMutation(checkSlugOptions(authClient))
```

## Params

<auto-type-table path="../../../../../../packages/react/src/hooks/mutations/use-check-slug.ts" name="CheckSlugParams" />
