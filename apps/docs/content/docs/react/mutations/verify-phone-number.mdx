---
title: useVerifyPhoneNumber
description: Verify a phone code or update the current user's phone number.
---

```tsx
import {
  type PhoneNumberAuthClient,
  useAuth,
  useVerifyPhoneNumber
} from "@better-auth-ui/react"

const { authClient } = useAuth()
const { mutate: verifyPhoneNumber } = useVerifyPhoneNumber(
  authClient as PhoneNumberAuthClient
)

verifyPhoneNumber({
  phoneNumber: "+12025550123",
  code: "123456"
})
```

Pass `updatePhoneNumber: true` while authenticated to replace the current user's phone number. The mutation refreshes session data after success.

## Params

<auto-type-table path="../../../../../../packages/react/src/mutations/phone-number/verify-phone-number-mutation.ts" name="VerifyPhoneNumberParams" />
