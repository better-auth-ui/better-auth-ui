---
title: <SecuritySettings />
description: A container component that wraps security-related settings like password management.
---

## Usage

<ComponentPreview name="shadcn-security-settings" />

```tsx file=<rootDir>/src/demos/shadcn/settings/security/security-settings.tsx
```

## Installation

```npm
npx shadcn@latest registry add @better-auth-ui=https://better-auth-ui.com/r/{style}/{name}.json
```

```npm
npx shadcn@latest add @better-auth-ui/security-settings
```

## Props

<auto-type-table path="../../../../../../../../examples/start-shadcn-example/src/components/auth/settings/security/security-settings.tsx" name="SecuritySettingsProps" />
## Fresh-session checks

Sensitive session operations can return `SESSION_NOT_FRESH`. The active sessions card shows an inline password prompt for this response. After the user signs in again, it retries the session query. If password sign-in is disabled, the prompt links to the configured sign-in route.
