---
icon: ShieldAlert
title: Captcha
description: Add bot protection to sign-up, sign-in, and password reset using a provider-agnostic captcha widget.
---

import { Step, Steps } from 'fumadocs-ui/components/steps'

<include>../../../shared/ui/plugins/captcha.mdx#shared-overview</include>

<include>../../../shared/ui/plugins/captcha.mdx#shared-social-sign-in</include>

## Setup

The authentication forms already contain the captcha widget slot. Configure the Better Auth server plugin and register the client plugin with your widget.

<Steps>
<Step>

<include>../../../shared/ui/plugins/captcha.mdx#shared-install-the-better-auth-plugin</include>

<include>../../../shared/ui/plugins/captcha.mdx#shared-protected-endpoints</include>

<include>../../../shared/ui/plugins/captcha.mdx#shared-endpoints-example</include>

</Step>
<Step>

<include>../../../shared/ui/plugins/captcha.mdx#shared-register-the-ui-plugin</include>

```tsx title="components/providers.tsx"
import { captchaPlugin } from "@better-auth-ui/react/plugins/captcha" // [!code highlight]
import { AuthProvider } from "@/components/auth/auth-provider"

import { TurnstileWidget } from "@/components/turnstile-widget" // [!code highlight]

<AuthProvider
  authClient={authClient}
  navigate={navigate}
  plugins={[captchaPlugin({ render: TurnstileWidget })]} // [!code highlight]
>
  {children}
</AuthProvider>
```

<include>../../../shared/ui/plugins/captcha.mdx#shared-react-render-component</include>

</Step>
</Steps>

<include>../../../shared/ui/plugins/captcha.mdx#shared-providers</include>

```tsx title="components/providers.tsx"
import { captchaPlugin } from "@better-auth-ui/react/plugins/captcha"
import { AuthProvider } from "@/components/auth/auth-provider"

import { TurnstileWidget } from "@/components/turnstile-widget"

<AuthProvider
  authClient={authClient}
  plugins={[captchaPlugin({ render: TurnstileWidget })]}
>
  {children}
</AuthProvider>
```

<include>../../../shared/ui/plugins/captcha.mdx#shared-h-captcha</include>

```tsx title="components/providers.tsx"
import { captchaPlugin } from "@better-auth-ui/react/plugins/captcha"
import { AuthProvider } from "@/components/auth/auth-provider"

import { HCaptchaWidget } from "@/components/hcaptcha-widget"

<AuthProvider
  authClient={authClient}
  plugins={[captchaPlugin({ render: HCaptchaWidget })]}
>
  {children}
</AuthProvider>
```

<include>../../../shared/ui/plugins/captcha.mdx#shared-captcha-fox</include>

```tsx title="components/providers.tsx"
import { captchaPlugin } from "@better-auth-ui/react/plugins/captcha"
import { AuthProvider } from "@/components/auth/auth-provider"

import { CaptchaFoxWidget } from "@/components/captchafox-widget"

<AuthProvider
  authClient={authClient}
  plugins={[captchaPlugin({ render: CaptchaFoxWidget })]}
>
  {children}
</AuthProvider>
```

## Options

<type-table path="../../../../../../packages/react/src/plugins/captcha-plugin.tsx" name="CaptchaPluginOptions" />

## Render props

The `render` component receives:

<type-table path="../../../../../../packages/react/src/plugins/captcha-plugin.tsx" name="CaptchaRenderProps" />

<include>../../../shared/ui/plugins/captcha.mdx#shared-render-props</include>
