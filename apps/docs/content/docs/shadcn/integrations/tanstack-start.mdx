---
title: TanStack Start
description: Integrate Better Auth UI with TanStack Start
icon: TanStackStart
---

import { Step, Steps } from 'fumadocs-ui/components/steps';

## Prerequisites

Complete the [Quick Start](/docs/shadcn) guide first.

## Integration

<Steps>
<Step>

<include>../../../shared/ui/integrations/tanstack-start.mdx#shared-configure-auth-provider</include>

```tsx title="components/providers.tsx" file=<rootDir>/../../examples/start-shadcn-example/src/components/providers.tsx
```

The `navigate` and `Link` props connect Better Auth UI to TanStack Router. Pass the `navigate` function directly because it accepts `{ to, replace }`.

`Link` requires a small wrapper. Better Auth UI passes the destination through `href`, but TanStack Router uses `to`.

Map `href` to `to`:

```tsx
Link={({ href, ...props }) => <Link to={href} {...props} />}
```

Do not pass TanStack Router's `Link` directly as `Link={Link}`. It does not receive the required `to` value.

Without this value, the anchor resolves against the current route. The hover URL is incorrect, although the link still works.

</Step>
<Step>

<include>../../../shared/ui/integrations/tanstack-start.mdx#shared-update-the-root-route</include>

```tsx title="routes/__root.tsx" file=<rootDir>/../../examples/start-shadcn-example/src/routes/__root.tsx
```

</Step>
<Step>
### Create the Auth Page

Install the auth components. Then create a dynamic auth page that selects the authentication view from the path.

<include>../../../shared/ui/installation/shadcn.mdx#shared-auth</include>

```tsx title="routes/auth/$path.tsx" file=<rootDir>/../../examples/start-shadcn-example/src/routes/auth/$path.tsx
```

<include>../../../shared/ui/integrations/tanstack-start.mdx#shared-create-the-auth-page</include>

</Step>
<Step>
### Create the Settings page

If you installed the `settings` component, create a dynamic settings route for the URL segment.

<include>../../../shared/ui/integrations/tanstack-start.mdx#shared-create-the-settings-page</include>

<include>../../../shared/ui/installation/shadcn.mdx#shared-settings</include>

```tsx title="routes/settings/$path.tsx" file=<rootDir>/../../examples/start-shadcn-example/src/routes/settings/$path.tsx
```

The `viewPaths.settings` object contains valid settings path segments: `account` and `security`.

</Step>
</Steps>

<include>../../../shared/ui/integrations/tanstack-start.mdx#shared-protecting-routes</include>

<include>../../../shared/ui/integrations/tanstack-start.mdx#shared-server-rendered-routes-before-load</include>

<include>../../../shared/ui/integrations/tanstack-start.mdx#shared-route-session-context</include>

<include>../../../shared/ui/integrations/tanstack-start.mdx#shared-session-hydration</include>

<include>../../../shared/ui/integrations/tanstack-start.mdx#shared-reactive-protection-and-prerendered-routes-use-authenticate</include>

```tsx title="routes/dashboard.tsx"
import { authClient } from "@/lib/auth-client"
import { useAuthenticate } from "@better-auth-ui/react"
import { createFileRoute, Link } from "@tanstack/react-router"

import { Spinner } from "@/components/ui/spinner"

export const Route = createFileRoute("/dashboard")({
  component: Dashboard
})

function Dashboard() {
  const { data: session } = useAuthenticate(authClient)

  if (!session) {
    return (
      <div className="flex justify-center my-auto">
        <Spinner color="current" />
      </div>
    )
  }

  return (
    <div className="flex flex-col items-center my-auto">
      <h1 className="text-2xl">Hello, {session.user.email}</h1>

      <Link to="/auth/$path" params={{ path: "sign-out" }}>
        Sign Out
      </Link>
    </div>
  )
}
```

<include>../../../shared/ui/integrations/tanstack-start.mdx#shared-session-changes</include>

## Example Project

For a complete working example, see [start-shadcn-example](https://github.com/better-auth-ui/better-auth-ui/tree/main/examples/start-shadcn-example) in the repository.

<include>../../../shared/ui/integrations/tanstack-start.mdx#shared-next-steps</include>

<Cards>
  <Card icon={<React />} title="React Reference" href="/docs/react">
    Hooks, queries, and mutations for every Better Auth endpoint.
  </Card>
  <Card icon={<React />} title="Queries" href="/docs/react/queries">
    Every auth read, with usage and server-side recipes.
  </Card>
  <Card icon={<React />} title="Mutations" href="/docs/react/mutations">
    Every auth write, with mutation keys and cache side effects.
  </Card>
</Cards>
