---
title: Next.js
description: Integrate Better Auth UI with Next.js
icon: NextJS
---

import { Step, Steps } from 'fumadocs-ui/components/steps'

## Prerequisites

Complete the [Quick Start](/docs/shadcn) guide first.

## Integration

<Steps>
<Step>

<include>../../../shared/ui/integrations/nextjs.mdx#shared-create-the-query-client</include>

```ts title="lib/query-client.ts" file=<rootDir>/../../examples/next-shadcn-example/src/lib/query-client.ts
```

<include>../../../shared/ui/integrations/nextjs.mdx#shared-query-client-lifetime</include>

</Step>
<Step>

<include>../../../shared/ui/integrations/nextjs.mdx#shared-configure-auth-provider</include>

```tsx title="components/providers.tsx" file=<rootDir>/../../examples/next-shadcn-example/src/components/providers.tsx
```

The `navigate` and `Link` props connect Better Auth UI to Next.js navigation. The `navigate` prop accepts `{ to, replace }` options.

</Step>
<Step>

<include>../../../shared/ui/integrations/nextjs.mdx#shared-update-the-root-layout</include>

```tsx title="app/layout.tsx" file=<rootDir>/../../examples/next-shadcn-example/src/app/layout.tsx
```

</Step>
<Step>
### Create the Auth Page

Install the auth components. Then create a dynamic auth page that selects the authentication view from the path.

<include>../../../shared/ui/installation/shadcn.mdx#shared-auth</include>

```tsx title="app/auth/[path]/page.tsx" file=<rootDir>/../../examples/next-shadcn-example/src/app/auth/[path]/page.tsx
```

The `viewPaths.auth` object contains the built-in auth paths: `redirect`, `sign-in`, `sign-up`, `sign-out`, `forgot-password`, `reset-password`, `reset-link-sent`, and `verify-email`.

</Step>
<Step>
### Create the Settings page

If you installed the `settings` component, create a dynamic settings route for the URL segment.

<include>../../../shared/ui/integrations/nextjs.mdx#shared-create-the-settings-page</include>

<include>../../../shared/ui/installation/shadcn.mdx#shared-settings</include>

```tsx title="app/settings/[path]/page.tsx" file=<rootDir>/../../examples/next-shadcn-example/src/app/settings/[path]/page.tsx
```

The `viewPaths.settings` object contains valid settings path segments: `account` and `security`.

</Step>
</Steps>

<include>../../../shared/ui/integrations/nextjs.mdx#shared-protecting-routes</include>

See the [example header](https://github.com/better-auth-ui/better-auth-ui/blob/main/examples/next-shadcn-example/src/components/header.tsx) for the complete pattern.

<include>../../../shared/ui/integrations/nextjs.mdx#shared-reactive-protection-and-prerendered-routes-use-authenticate</include>

```tsx title="app/dashboard/page.tsx"
"use client"

import { authClient } from "@/lib/auth-client"
import { useAuthenticate } from "@better-auth-ui/react"
import Link from "next/link"

import { Spinner } from "@/components/ui/spinner"

export default function Dashboard() {
  const { data: session } = useAuthenticate(authClient)

  if (!session) {
    return (
      <div className="flex justify-center my-auto">
        <Spinner color="current" />
      </div>
    )
  }

  return (
    <div className="flex flex-col items-center my-auto">
      <h1 className="text-2xl">Hello, {session.user.email}</h1>

      <Link href="/auth/sign-out">Sign Out</Link>
    </div>
  )
}
```

<include>../../../shared/ui/integrations/nextjs.mdx#shared-session-changes</include>

## Example Project

For a complete working example, see [next-shadcn-example](https://github.com/better-auth-ui/better-auth-ui/tree/main/examples/next-shadcn-example) in the repository.

<include>../../../shared/ui/integrations/nextjs.mdx#shared-next-steps</include>

<Cards>
  <Card icon={<React />} title="React Reference" href="/docs/react">
    Hooks, queries, and mutations for every Better Auth endpoint.
  </Card>
  <Card icon={<React />} title="Queries" href="/docs/react/queries">
    Every auth read, with usage and server-side recipes.
  </Card>
  <Card icon={<React />} title="Mutations" href="/docs/react/mutations">
    Every auth write, with mutation keys and cache side effects.
  </Card>
</Cards>
