---
title: resetPhoneNumberPasswordOptions
description: Create a Solid mutation that resets a phone credential password.
---

## Options factory

```tsx
import {
  type PhoneNumberAuthClient,
  resetPhoneNumberPasswordOptions,
  useAuth
} from "@better-auth-ui/solid"
import { createMutation } from "@tanstack/solid-query"

const { authClient } = useAuth()
const resetPassword = createMutation(() =>
  resetPhoneNumberPasswordOptions(authClient as PhoneNumberAuthClient)
)

resetPassword.mutate({
  phoneNumber: "+12025550123",
  otp: "123456",
  newPassword
})
```

## Params

<auto-type-table path="../../../../../../packages/solid/src/mutations/phone-number/reset-phone-number-password-mutation.ts" name="ResetPhoneNumberPasswordParams" />
