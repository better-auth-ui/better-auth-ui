<DocVariant name="react">
This operation is a mutation because a user action starts it. Typical actions include a debounced input change or form submission.

`data` is typed as `{ available: boolean; message: string | null }`.
</DocVariant>

<DocVariant name="solid">
Requires the Better Auth username plugin. Import your configured `authClient`. It already carries the username methods from `createAuthClient`.

This check is modeled as a mutation because callers usually trigger it from user input, not from route-level data loading. Returned data is typed as `{ available: boolean; message: string | null }`.
</DocVariant>

<MutationPage id="is-username-available">

<PageSlot name="usage" doc-variant="react">

`useIsUsernameAvailable` requires the Better Auth `username` plugin. Import your configured `authClient`. It already carries the plugin methods from `createAuthClient`.

</PageSlot>

</MutationPage>
