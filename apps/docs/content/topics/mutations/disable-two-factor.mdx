<DocVariant name="solid">
<include>../../shared/mutations/plugin-requirements.mdx#shared-two-factor</include>
</DocVariant>

Refetches the session so `user.twoFactorEnabled` is up to date.

<MutationPage id="disable-two-factor">

<PageSlot name="usage" doc-variant="react">

`useDisableTwoFactor` requires the Better Auth `twoFactor` plugin. Cast `authClient` to `TwoFactorAuthClient` so TypeScript picks up the plugin-extended method signature.

</PageSlot>

</MutationPage>
