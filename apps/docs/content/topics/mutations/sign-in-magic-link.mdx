<DocVariant name="react">
The session is established when the user clicks the emailed link, so no session-cache side effects run here.
</DocVariant>

<DocVariant name="solid">
Requires the Better Auth magic-link plugin. Import your configured `authClient`. It already carries the magic-link methods from `createAuthClient`.
</DocVariant>

<MutationPage id="sign-in-magic-link">

<PageSlot name="usage" doc-variant="react">

`useSignInMagicLink` requires the Better Auth `magicLink` plugin. Import your configured `authClient`. It already carries the plugin methods from `createAuthClient`.

</PageSlot>

</MutationPage>
