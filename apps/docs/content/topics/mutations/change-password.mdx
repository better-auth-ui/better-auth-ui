<DocVariant name="react">
The server uses `revokeOtherSessions` to decide whether it revokes existing sessions. This hook does not update the cache.
</DocVariant>

<MutationPage id="change-password" />
