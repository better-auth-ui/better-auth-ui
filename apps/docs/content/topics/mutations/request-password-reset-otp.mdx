<DocVariant name="react">
Pair with `useResetPasswordOtp`, which takes the code and the new password together: no reset link is involved.
</DocVariant>

<DocVariant name="solid">
<include>../../shared/mutations/plugin-requirements.mdx#shared-email-otp</include>

Pair with `resetPasswordOtpOptions`, which takes the code and the new password together: no reset link is involved.
</DocVariant>

<MutationPage id="request-password-reset-otp">

<PageSlot name="usage" doc-variant="react">

`useRequestPasswordResetOtp` requires the Better Auth `emailOTP` plugin. Cast `authClient` to `EmailOtpAuthClient` so TypeScript picks up the plugin-extended method signature.

</PageSlot>

</MutationPage>
